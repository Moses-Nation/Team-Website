apiVersion: apps/v1
kind: Deployment
metadata:
  name: moses-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: moses-pod
  template:
    metadata:
      labels:
        app: moses-pod
    spec:
      containers:
        - name: moses-pod
          image: gcr.io/constellation-275522/teamwebsite
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: ".5"
              memory: "500Mi"
            requests:
              cpu: ".25"
              memory: "250Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: mosespod-service
spec:
  selector:
    app: moses-pod
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000